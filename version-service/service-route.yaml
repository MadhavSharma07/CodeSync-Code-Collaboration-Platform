apiVersion: v1
kind: Service
metadata:
  name: version-service
  labels:
    app: version-service
    project: codesync
spec:
  selector:
    app: version-service
  ports:
    - name: http
      port: 8086
      targetPort: 8086
      protocol: TCP
  type: ClusterIP
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: version-service
  labels:
    app: version-service
    project: codesync
spec:
  to:
    kind: Service
    name: version-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
